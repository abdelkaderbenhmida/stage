# app/catalog/

Namespace directory for the "catalog" family of demo services. It currently holds no code
of its own — only the `items/` sub-service — and exists purely as a grouping folder so
catalog-related services can be discovered together under one path prefix.

- `items/` — the `catalog-items` FastAPI service (see its own README for details).
