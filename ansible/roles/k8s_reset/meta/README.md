# meta

Galaxy-compatible role metadata for `k8s_reset`.

- `main.yml` — `galaxy_info` describing the role as "Reset Kubernetes — kubeadm reset +
  iptables/ipvs flush + CNI cleanup", MIT license, `min_ansible_version: "2.14"`, Ubuntu
  jammy/noble support, no role dependencies.
