# meta

Galaxy-compatible role metadata for `k8s_common`.

- `main.yml` — `galaxy_info` describing the role as applying "kubeadm prerequisites
  (swap, kernel modules, sysctl, CRI socket)", MIT license, `min_ansible_version: "2.14"`,
  Ubuntu jammy/noble support, no role dependencies.
