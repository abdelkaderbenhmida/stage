# meta

Galaxy-compatible role metadata for `docker`.

- `main.yml` — `galaxy_info` (author "DevOps Platform Team", description, MIT license,
  `min_ansible_version: "2.14"`, supported platforms Ubuntu jammy/noble) and an empty
  `dependencies` list (this role has no role dependencies).
