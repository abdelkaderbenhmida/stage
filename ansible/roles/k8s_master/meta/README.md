# meta

Galaxy-compatible role metadata for `k8s_master`.

- `main.yml` — `galaxy_info` describing the role as "Bootstrap kubeadm master + Calico
  Tigera operator + generate worker join command", MIT license,
  `min_ansible_version: "2.14"`, Ubuntu jammy/noble support, no role dependencies.
