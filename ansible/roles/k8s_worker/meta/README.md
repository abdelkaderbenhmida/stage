# meta

Galaxy-compatible role metadata for `k8s_worker`.

- `main.yml` — `galaxy_info` describing the role as "Join Kubernetes worker nodes to the
  kubeadm master (serial: 1)", MIT license, `min_ansible_version: "2.14"`, Ubuntu
  jammy/noble support, no role dependencies.
