apiVersion: v1
kind: Service
metadata:
  name: products-service
  namespace: devops-platform
  labels:
    app.kubernetes.io/name: products-service
    app.kubernetes.io/part-of: devops-platform
spec:
  selector:
    app.kubernetes.io/name: products-service
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  type: ClusterIP
---
# ServiceMonitor for products-service. Prometheus (in monitoring ns) scrapes
# the Service via its named port "http" (= port 80 -> containerPort 8000).
# Path /metrics emitted by prometheus-fastapi-instrumentator.
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: products-service
  namespace: monitoring
  labels:
    app.kubernetes.io/name: products-service
    app.kubernetes.io/part-of: devops-platform
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: products-service
  namespaceSelector:
    matchNames:
      - devops-platform
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
