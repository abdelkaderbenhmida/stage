{{/*
  Ressource temoin pour la demonstration du prune ArgoCD (scenario du rapport).
  Elle est ajoutee dans Git, synchronisee automatiquement, puis retiree : ArgoCD
  doit alors la supprimer du cluster sans intervention manuelle.
*/}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-prune
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: demo-prune
    app.kubernetes.io/part-of: devops-platform
data:
  note: "ressource temoin ajoutee pour verifier le prune automatique"
