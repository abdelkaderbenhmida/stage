apiVersion: v2
name: devops-platform-apps
description: Generic chart for the DevOps Central Platform microservices. Renders one Deployment/Service/SA/HPA/PDB per entry in .Values.services. Service discovery is external (CI dir scan, ArgoCD ApplicationSet) — this chart knows no service names.
type: application
version: 0.1.0
appVersion: "1.0"