# Alertmanager instance managed by Prometheus Operator.
# 1 replica, PVC 2Gi for notification state, ns=monitoring.
# alertmanagerConfiguration.name wires in the AlertmanagerConfig CR from
# alertmanager-config.yaml (must live in this same namespace). Preferred
# over alertmanagerConfigSelector: the selector path makes the operator
# inject a namespace="monitoring" matcher into every route, silently
# dropping alerts labelled with other namespaces.
---
apiVersion: monitoring.coreos.com/v1
kind: Alertmanager
metadata:
  name: alertmanager
  namespace: monitoring
  labels:
    app.kubernetes.io/name: alertmanager
    app.kubernetes.io/part-of: devops-platform
spec:
  alertmanagerConfiguration:
    name: alertmanager
  image: quay.io/prometheus/alertmanager:v0.27.0
  version: v0.27.0
  replicas: 1
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi
  storage:
    volumeClaimTemplate:
      spec:
        storageClassName: local-path
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 2Gi
  securityContext:
    runAsNonRoot: true
    runAsUser: 65534
    fsGroup: 65534
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: alertmanager
      securityContext:
        readOnlyRootFilesystem: true
        capabilities:
          drop: [ALL]
        allowPrivilegeEscalation: false
