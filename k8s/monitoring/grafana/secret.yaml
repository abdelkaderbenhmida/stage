# Grafana admin credentials Secret.
# Values injected out-of-band via bootstrap script.
# INJECT-VIA-Scripts-bootstrap-grafana — placeholder values fail at runtime.
---
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-credentials
  namespace: monitoring
  labels:
    app.kubernetes.io/name: grafana-admin-credentials
    app.kubernetes.io/part-of: devops-platform
type: Opaque
stringData:
  admin-user: "INJECT-VIA-Scripts-bootstrap-grafana"
  admin-password: "INJECT-VIA-Scripts-bootstrap-grafana"
